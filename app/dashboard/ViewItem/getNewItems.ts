
// import { GetNewItems } from "@/actions/items.action";

// export default async function getLatestItems(CardCode: string) {
//     const interval = 1000;

//     setInterval(() => {
//         const result = await GetNewItems(CardCode);

//         if (result) { return }
//     }, interval);
// }